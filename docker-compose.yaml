version: "3.8"
services:
  main:
    build: .
    container_name: discord-twitch-bot
    volumes:
      - $HOME/mybot-data:/app/data
    env_file:
      - path: ./.env.production
        required: true
    environment:
      - DISCORD_TOKEN
      - DISCORD_CLIENTID